import 'package:flutter/material.dart';

class AsistenceForm extends StatelessWidget {
  const AsistenceForm({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
